# Minimal requirements for Cosmos-Prefer2.5 inside NVIDIA NeMo Docker
# Most dependencies (PyTorch, transformers, etc.) are already included in nvcr.io/nvidia/nemo:25.11
natsort 

# Checkpoint downloader dependencies
rich>=13.7.0
huggingface-hub>=0.36.0

# Video and image processing (if not already in NeMo)
imageio[pyav,ffmpeg]>=2.37.0
av>=16.0.1
mediapy>=1.2.4

# Additional utilities
loguru>=0.7.3
einops>=0.8.1
safetensors>=0.6.2

# Development tools
jupyter>=1.0.0
ipython>=8.20.0
ipdb>=0.13.13

# Optional: Cosmos-specific tools (uncomment if needed)
# diffusers>=0.35.2
# peft>=0.17.1
# albumentations>=2.0.8
# opencv-python>=4.11.0.86
# scikit-image>=0.25.2
# pycocotools>=2.0.10
# wandb>=0.22.2
# sam2>=1.1.0
